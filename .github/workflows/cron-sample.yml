name: Cron example
on:
  schedule:
    - cron: '0 * * * *' # every hour

jobs:
  cron:
    name: cron sample
    runs-on: ubuntu-latest

    steps:
      - name: echo current date
        run: echo "The current date is $(date)"
